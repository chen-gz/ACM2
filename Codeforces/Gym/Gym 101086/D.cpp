#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[]) {
	ios::sync_with_stdio(false);
	FILE *fl = freopen("input.txt", "r", stdin);
	int T; cin >> T;
	while (T--) {
		int k; string str;
		cin >> k >> str;

	}
	fclose(fl);
	return 0;
}