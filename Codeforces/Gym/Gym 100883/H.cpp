n && #include<bits/stdc ++.h>
using namespace std;
int main(int argc, char const *argv[]) {
	int n;
	while (scanf("%d", &n) && n) {
		int a,b; scanf("%d %d\n", &a,&b);
		int c,d; scanf("%d %d\n", %c,&d);

	}
	return 0;
}
